[Unit]
Description=Classroom proxy egress deny guards
Before=xray-lab.service

[Service]
Type=oneshot
ExecStart=/usr/sbin/nft -f /etc/classlab/proxy-guard.nft
RemainAfterExit=yes
# No ExecStop: stopping the unit does not open an unintended fallback path.

[Install]
WantedBy=multi-user.target
